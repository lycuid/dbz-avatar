import styled from 'styled-components';

const StyledSwatchContainer = styled.div`
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  justify-items: center;
  margin: 10px 0;
`;

export default StyledSwatchContainer;
