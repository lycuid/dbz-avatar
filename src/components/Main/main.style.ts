import styled from 'styled-components';
import { Container } from '../../style/global';

const StyledMain = styled(Container).attrs({ as: 'main' })`
  width: 100%;
  flex: 1;
`;

export default StyledMain;
